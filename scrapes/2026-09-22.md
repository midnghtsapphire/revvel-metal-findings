# Scrape 2026-09-22 (America/Denver)

Weekday metal scrape. Prior HEAD `7fb8124abd78a21dffd833a0412ec6fb8224fd37` (32 findings).

## Acquired / updated

| id | action | lane | note |
| --- | --- | --- | --- |
| cfgzip | updated | 01-deterministic-ai | Added live `coli-saar/cfgzip-rust` companion (Rust `cfgzip-preprocess`); still no vLLM/SGLang path |

## Verified URLs

- https://github.com/mjs227/cfgzip (live; 4★; Apache-2.0)
- https://github.com/coli-saar/cfgzip-rust (live; Apache-2.0; Rust preprocessor CLI)
- https://pypi.org/project/cfgzip/ (`cfgzip==0.1.1`)
- https://github.com/Gompyn/PSC (still live; still research harness `fast_constraint`, no production vLLM plugin)
- https://docs.vllm.ai/en/latest/features/structured_outputs/ (backends still xgrammar / guidance; **no PSC**)
- https://github.com/guidance-ai/llguidance (spot-check alive; ~873★)
- https://github.com/mlc-ai/xgrammar (spot-check alive; ~1921★)
- https://github.com/IsaacBreen/glrmask (spot-check alive)
- https://github.com/nradawg/vocab-mask-trie (spot-check alive; still 0★)
- https://registry.npmjs.org/vocab-mask-trie — **404** (still unpublished)
- https://github.com/jpstone/spec-guard (spot-check alive)
- https://icml.cc/virtual/2026/poster/62392 (Gram2Token poster still live)
- https://arxiv.org/abs/2507.16768 (WGrammar paper still live)

## Dropped / dead this pass

- https://github.com/wrran/wgrammar — still **404**. Paper live (arXiv 2507.16768); no code tree; not added.
- https://github.com/Paradozile/Gram2Token — still **404**. ICML 2026 poster still claims this URL; paper/poster only; **not added**.
- No existing catalogued finding marked dropped (core URLs above still resolve).

## Considered / skipped

- **devChaudhari7/schemalock** — from-scratch byte-level JSON-Schema automaton + transformers mask (created 2026-09-22). Solid pedagogy and CORD bench, but pure-Python rebuild of ground already covered by **llguidance / xgrammar-2 / glrmask / outlines**; no vLLM/SGLang path; skipped.
- **Zigerus/interlock-mcp** — hash-bound propose→countersign→stage-execute MCP (Apache-2.0, alpha). Strong deliver-path shape, but overlaps **spec-guard** Approve/Authorize pattern; `pip install interlock-mcp` name collides with unrelated PyPI `tanwar-div/interlock-agent-control-plane` fuse; skipped pending clean publish identity.
- **velvetmonkey/seal** (+ archived `mcp-seal`) — Claude Code exact-call approval gate with signed receipts. Same shell/Claude-Code surface as previously skipped **hashgate**; skipped.
- **dsh0416/transformers-llguidance** — npm `transformers-llguidance@0.2.1` (MIT): llguidance WASM for transformers.js. Thin wrap of already-catalogued **llguidance**; browser/transformers.js path, not a Node zero-dep coder mask (see **vocab-mask-trie**); WASM often needs local rebuild; skipped.
- **coli-saar/cfgzip-rust** — not a new finding; recorded as URL update under **cfgzip** (same package family / PyPI `cfgzip`).
- **Seppelllo/hashgate**, **salmanneedsajob/notary-mcp**, **gibbon/fixedcode**, **STATIC**, **Trie Automata**, **Rel(AI)Build**, **Effect-TS DST** (PR #6216 merged May 2026 but no clear `@effect/dst` publish hit), **pooyagolchian/specforge**, **mrmaxsteel/mindspec**, **s977043/PlanGate**, **nradawg/mask-state-rollback**, **EPIC-Decoding**, **greatgramma**, **glideapps/determined**, **formatron/kbnf**, **marionette** — still skipped (same reasons as prior scrapes).

## Still hunting

- Production PSC drop-in for vLLM (vLLM structured outputs still XGrammar/guidance; Gompyn/PSC remains bench harness)
- WGrammar / Gram2Token code drops if repos reappear
- vocab-mask-trie npm publish (GitHub-only today; registry 404)
- CFGzip vLLM/SGLang serving path (today transformers LogitsProcessor + XGrammar2 only; Rust preprocess does not change that)
- OpenHands / Extism / Zig version bumps only if release notes move the needle
- Do not open a second pipeline in revvel-standards

## Rank shift

None. Tree stays **32** findings. `cfgzip` remains rank **8** with a stronger preprocess story; serving gap unchanged.
